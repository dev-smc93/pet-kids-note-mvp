# Database (Supabase)
DATABASE_URL="postgresql://postgres.[프로젝트ref]:[비밀번호]@db.xxx.supabase.co:5432/postgres"

# Supabase Auth (Settings > API > Project URL, anon public key)
NEXT_PUBLIC_SUPABASE_URL="https://[프로젝트ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Seed (시드 실행 시 필요)
# Supabase Auth에서 테스트 계정 생성 후, Authentication > Users에서 user_id(UUID) 복사
SEED_USER_ID=""
